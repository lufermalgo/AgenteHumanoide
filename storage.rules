rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Durante desarrollo con emuladores, permitir todo
      allow read, write: if true;
    }
  }
}