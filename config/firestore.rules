rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Durante desarrollo con emuladores, permitir todo
      allow read, write: if true;
    }
  }
}